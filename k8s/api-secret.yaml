# DEMO uniquement. APP_SECRET et MONGODB_URL doivent etre injectes
# via un gestionnaire de secrets en production.
apiVersion: v1
kind: Secret
metadata:
  name: api-secret
  namespace: electronics
  labels:
    app: electronics-api
type: Opaque
stringData:
  APP_SECRET: "remplacer-par-32-caracteres-aleatoires"
  # user:pass identiques au Secret mongodb-credentials ; authSource=admin
  # car l'utilisateur root est cree dans la base "admin".
  MONGODB_URL: "mongodb://root:S3cureChangeMe@mongodb.electronics.svc.cluster.local:27017/?authSource=admin"
